{
  "name": "airlock-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
      "dev": "nodemon --watch '**/*.ts' --exec 'ts-node' index.ts",
	  "generate-schema": "generate-airtable-schema"
  },
  "author": "",
  "dependencies": {
    "airlock-server": "^0.2.3",
    "tslib": "^2.0.3"
  },
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.6",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.5"
  },
  "airtable-schema-generator": {
    "mode": "Auto",
    "output": "./airtable/"
  }
}
